<!DOCTYPE html>
<html lang="en"

      xmlns:custom="http://www.w3.org/1999/xhtml"
      xmlns:th="http://www.thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout"
      layout:decorate="_layout">
<head>
  <meta charset="UTF-8">
  <title>List product</title>
</head>
<body>
<th:block th:replace="_layout"></th:block>
<div class="container body-content" layout:fragment="content">
  <a class="btn btn-primary" th:href="@{products/create}">Create New Product</a>
  <table class="table table-striped">
    <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">Image</th>
      <th scope="col">Price</th>
      <th scope="col"></th>
    </tr>
    </thead>
    <tbody>
    <tr th:each="product: ${listproduct}">
      <th scope="row" th:text="${product.id}"></th>
      <td th:text="${product.name}"></td>
      <td>
        <img heigh = "120" width="150" th:src = "@{/image/} + ${product.image}" >
      </td>
      <td th:text="${product.price}"></td>
      <td>
        <a class="btn btn-secondary" custom:linkMethod="post"
           th:href="@{/products/edit/{id}(id=${product.id})}">Edit</a>
        <a class="btn btn-danger" custom:linkMethod="post" th:href="@{/products/delete/{id}(id=${product.id})}"
        >Delete</a>
      </td>
    </tr>
    </tbody>
  </table>
</div>


</body>
</html>